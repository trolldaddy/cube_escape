# Google Sites 部署說明（Cube Escape）

## 推薦方式（最穩定）
1. 將 `index.html` 放到可公開網址（例如 GitHub Pages / Cloudflare Pages）。
2. 在 Google Sites 使用「嵌入 → URL」貼上該網址。
3. 調整嵌入框尺寸（建議寬 1200、高 900 以上）。

> 這個方式最穩定，因為完整頁面會在自己的來源執行，避免部分 iframe 安全限制。

## 直接在 Google Sites 內嵌 HTML（可用，但可能受限）
1. Google Sites → 插入 → 嵌入 → 嵌入程式碼。
2. 貼上 `index.html` 內容。
3. 發布後測試。

若遇到儲存受限：
- 畫面會出現紫色提示「目前執行環境限制了本機儲存，改用暫存記憶體模式」。
- 請使用左側「匯出 JSON / 匯入 JSON」做回合備份與還原。

## 備份流程建議
- 每個回合結束按一次「匯出 JSON」。
- 若重新整理或換裝置，按「匯入 JSON」即可回復。

## 常見問題
- **Q: 為什麼重新整理後資料不見？**
  - A: 代表該嵌入環境阻擋了 `localStorage`，請改用匯出/匯入流程。
- **Q: 點擊被遮擋？**
  - A: 在 Sites 把嵌入框高度調高，並確認不是頁面上其他區塊覆蓋。
